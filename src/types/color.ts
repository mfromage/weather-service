import {StyleSheet} from 'react-native';

export const Colors = {
  primary: 'blue',
  error: 'red',
  white: 'white',
  black: 'black',
  lightgray: 'lightgray',
};

export const colorStyles = StyleSheet.create({
  textError: {
    color: Colors.error,
  },
});
